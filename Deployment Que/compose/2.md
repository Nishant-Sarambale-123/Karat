7.	Identify the issues in below docker compose file?
version: '3'

services:
  db:
    image: postgres:16-alpine
    # container_name is missing
    container_name: postgres_db
    environment:
      POSTGRES_USER: myuser
      POSTGRES_PASSWORD: mypassword
      POSTGRES_DB: mydatabase
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
  nginx:
    image: nginx:alpine
    # container_name is missing
    container_name: nginx_web
    ports:
      - "80:80"
    depends_on:
      - db
    volumes:
      # Mount your local application code/static files into the Nginx serving directory
      # Replace './html' with the path to your static files or application root
      - ./html:/usr/share/nginx/html:ro 
# volumes missing in given file
volumes:
  postgres_data:
